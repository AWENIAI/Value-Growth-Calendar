# 阿文项目保护规则

## AI 修改限制

未经阿文在当前对话中明确回复确认允许，AI / Codex / 自动化助手不得擅自修改本项目任何内容。

## 强制确认验证

AI / Codex / 自动化助手在修改本项目之前，必须满足以下条件：

1. 当前对话里有阿文明确授权本次修改。
2. 修改范围、原因和预期文件已先说明。
3. 服务器上存在有效许可文件：/opt/awen-calendar-change-permit。
4. 许可文件必须包含当前项目路径。
5. 修改后必须运行最小有效验证。
6. 修改后必须同步回 GitHub；如果无法同步，必须明确报告阻塞点。

允许例外：

1. 项目已配置的 systemd timer 可按既定脚本自动更新日历数据与 ICS 文件。
2. 只读检查、状态查看、日志查看不算修改。
3. 用户明确要求“同步回 GitHub”“部署”“修复”“更新数据”等具体操作时，只能在该授权范围内修改。

禁止事项：

- 不得提交服务器密码、Token、私钥、node_modules、临时备份文件。
- 不得把脚本执行成功、HTTP 200、GitHub push 成功单独称为用户目标成功。
- 日历相关修改必须检查 DTSTART、DTEND、VALARM、LAST-MODIFIED 和公网订阅内容。
