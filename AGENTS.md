# 阿文项目保护规则

## AI 修改限制

未经阿文在当前对话中明确回复确认允许，AI / Codex / 自动化助手不得擅自修改本项目任何内容。

允许例外：

1. 项目已配置的服务器定时任务可以按既定脚本自动更新日历数据与 ICS 文件。
2. 用户明确要求“同步回 GitHub”“部署”“修复”“更新数据”等具体操作时，只能在该授权范围内修改。
3. 只读检查、状态查看、日志查看不算修改。

执行要求：

- 修改前必须说明将修改的文件和原因。
- 修改后必须同步回 GitHub，除非用户明确要求只保留在服务器。
- 不得提交服务器私有文件、密码、Token、临时备份文件或 node_modules。
